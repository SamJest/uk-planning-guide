# Phase 0 URL Change Set

## Added to the canary

- `/england/rules/article-4/colchester/`
- `/updates/phase-0-integrity-repair/` (noindex; excluded from sitemaps pending approval)

## Legacy bridge under test

- `/councils/colchester/` has a canonical link to `/england/councils/colchester/` and is noindex.
- The redirect registry records the intended permanent redirect for hosts that can return HTTP redirects.

No live URL was removed, redirected, deindexed or returned as 410 by Phase 0.
